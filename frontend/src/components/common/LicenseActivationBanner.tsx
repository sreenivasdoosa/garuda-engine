/**
 * License activation banner — no longer needed in per-entity licensing model.
 * Kept as a no-op component to avoid breaking imports in layouts.
 */
const LicenseActivationBanner: React.FC = () => {
  return null;
};

export default LicenseActivationBanner;
